apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: nginx
  name: nginx-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      run: nginx
  template:
    metadata:
      labels:
        run: nginx

    spec:

      volumes:
      - name: shared-volume
        emptyDir: {}



      containers:
      - image: 51
        name:  51
        imagePullSecrets:
        - name: eW91c3J5OTQzOnN0ZXZlbmJpbGw0NTYyMDA4

        volumeMounts:
        - name: shared-volume
          mountPath: /usr/share/nginx/html
